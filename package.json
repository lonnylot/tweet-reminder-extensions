{
  "name": "tweet-reminder",
  "version": "1.0.3",
  "description": "Set a reminder for a tweet.",
  "main": "index.js",
  "scripts": {
    "start": "web-ext run -v --bc",
    "sign": "web-ext sign --verbose --ignore-files=.gitignore .git .DS_Store --api-key=${WEBEXT_API_KEY} --api-secret=${WEBEXT_API_SECRET}",
    "build": "web-ext build --verbose --ignore-files=.gitignore .git .DS_Store node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lonny@nonstockproductions.com",
  "license": "MIT",
  "devDependencies": {
    "webextension-polyfill": "^0.3.1"
  }
}
